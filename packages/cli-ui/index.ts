/**
 * 🧠 AI File Brain — CLI UI
 * Public barrel export for the entire CLI UI toolkit.
 */

// Theme: colors, icons, layout helpers
export {
    brand, accent, success, warning, error, dim, muted, highlight, subtle,
    icons, line, thinLine,
    banner, sectionHeader, metric, summaryBox,
} from "./theme.js";

// Spinner
export { Spinner, spin } from "./spinner.js";

// Progress bars
export { ProgressBar, MultiProgress } from "./progress.js";

